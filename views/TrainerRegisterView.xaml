<UserControl x:Class="Pokedex.views.TrainerRegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:services="clr-namespace:Pokedex.services"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1600"
             Background="Transparent">

    <UserControl.Resources>
        <services:IntToBoolConverter x:Key="IntToBoolConverter" />
    </UserControl.Resources>

    <Grid>
        <Border Background="White" CornerRadius="20" Width="550" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="40,50">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="8" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="T R A I N E R   C A R D" FontSize="26" FontWeight="Black" Foreground="#2C2C2E" HorizontalAlignment="Center" Margin="0,0,0,35"/>

                <TextBlock Text="NAME" FontSize="13" FontWeight="Bold" Foreground="#888" Margin="0,0,0,5"/>
                <Border Background="#F8F8FA" CornerRadius="8" Margin="0,0,0,20">
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontSize="16" FontWeight="SemiBold" Foreground="#333" Padding="12" Background="Transparent" BorderThickness="0"/>
                </Border>

                <TextBlock Text="TRAINER ID" FontSize="13" FontWeight="Bold" Foreground="#888" Margin="0,0,0,5"/>
                <Border Background="#F8F8FA" CornerRadius="8" Margin="0,0,0,20">
                    <TextBox Text="{Binding TrainerId, UpdateSourceTrigger=PropertyChanged}" FontSize="16" FontWeight="SemiBold" Foreground="#333" Padding="12" Background="Transparent" BorderThickness="0" MaxLength="6"/>
                </Border>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="REGION" FontSize="13" FontWeight="Bold" Foreground="#888" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Regions}" SelectedItem="{Binding SelectedRegion}" FontSize="15" Padding="10" Background="#F8F8FA" BorderBrush="#E5E5EA" Foreground="#333" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="GENERATION" FontSize="13" FontWeight="Bold" Foreground="#888" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Generations}" SelectedItem="{Binding SelectedGeneration}" IsEnabled="{Binding Generations.Count, Converter={StaticResource IntToBoolConverter}}" FontSize="15" Padding="10" Background="#F8F8FA" BorderBrush="#E5E5EA" Foreground="#333" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="GAME" FontSize="13" FontWeight="Bold" Foreground="#888" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Games}" SelectedItem="{Binding SelectedGame}" IsEnabled="{Binding Games.Count, Converter={StaticResource IntToBoolConverter}}" FontSize="15" Padding="10" Margin="0,0,0,40" Background="#F8F8FA" BorderBrush="#E5E5EA" Foreground="#333" FontWeight="SemiBold"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="CANCEL" Click="btnCancel_Click" Background="#E5E5EA" Foreground="#555" FontWeight="Bold" FontSize="15" Height="50" Margin="0,0,10,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Grid.Column="1" Content="SAVE" Click="btnSave_Click" Background="#FFEB3B" Foreground="Black" FontWeight="Bold" FontSize="15" Height="50" Margin="10,0,0,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>