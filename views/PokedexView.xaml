<UserControl x:Class="Pokedex.views.PokedexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F5F5F7">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Pokemons}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="15" Margin="10" Width="180" Height="220">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                            </Border.Effect>

                            <StackPanel>
                                <TextBlock Text="{Binding Dex, StringFormat='#{0:D3}'}" 
                                           FontWeight="Bold" Foreground="Gray"/>

                                <Image Source="{Binding Image}" Height="100" Margin="0,10"/>

                                <TextBlock Text="{Binding Name}" FontSize="16" 
                                           FontWeight="Bold" HorizontalAlignment="Center"/>

                                <Border Background="#78C850" CornerRadius="5" Padding="10,2" 
                                        HorizontalAlignment="Center" Margin="0,5">
                                    <TextBlock Text="{Binding Types[0]}" Foreground="White" FontSize="12"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>