<UserControl x:Class="Pokedex.views.PokedexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#F5F5F7">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ListBox ItemsSource="{Binding Pokemons}" 
                     SelectedItem="{Binding SelectedPokemon}"
                     Background="Transparent" 
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Background="Transparent" BorderThickness="0" Cursor="Hand"
                                Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="White" 
                                            BorderBrush="{Binding Types, Converter={StaticResource TypeToBrushConverter}}" 
                                            BorderThickness="2" 
                                            CornerRadius="15" Margin="10" Width="170" Height="210">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                                        </Border.Effect>

                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Dex, StringFormat='#{0:D3}'}" 
                                                       FontSize="11" FontWeight="Bold" Foreground="Gray" 
                                                       Margin="12,8,0,0" HorizontalAlignment="Left"/>

                                            <Image Source="{Binding Image}" Height="90" Margin="0,5"/>

                                            <TextBlock Text="{Binding Name}" FontSize="16" 
                                                       FontWeight="Bold" HorizontalAlignment="Center" 
                                                       Foreground="#212121" Margin="0,5"/>

                                            <ItemsControl ItemsSource="{Binding Types}" HorizontalAlignment="Center" Margin="0,5">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Image Source="{Binding Converter={StaticResource TypeIconConverter}}" 
                                                               Height="20" Width="20" Margin="3,0"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</UserControl>